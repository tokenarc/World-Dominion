
## GitHub Superpowers
You have access to the `gh` CLI. You are fully authorized to:
- Use `gh issue list` to see what we need to build next.
- Use `gh pr create` to open Pull Requests when you finish a feature.
